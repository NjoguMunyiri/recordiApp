<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.james.recordiapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iclauncher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register" />
        <activity android:name=".home" />
        <activity android:name=".forumns" />
        <activity android:name=".forum" />
        <activity android:name=".myprofile" />
        <activity android:name=".upload" />
        <activity android:name=".createforumn" />
        <activity android:name=".homeartist" />
        <activity android:name=".listener"></activity>
    </application>

</manifest>
